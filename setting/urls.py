# Django Build-in
from django.urls import path

# Local Apps
from . import views

app_name = 'setting'

urlpatterns = [
    # path('social-media/', views.SocialMediaListView.as_view(), name='social-media'),
]
